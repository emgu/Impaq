package tests;

import static org.junit.Assert.*;
import static org.mockito.Mockito.*;
//import org.junit.Test;
//import org.junit.runner.RunWith;
import org.mockito.MockitoAnnotations.Mock;
//import org.mockito.Mock;

import org.junit.Assert;

import store.*;


public class tests {

	/*public static void test(){
	//	BarCodeScannerReturnsString();
		getBarCodeReturnsString();
		getBarCodeReturnsNotNull();
		getPriceReturnsNotNegative();
	}
	
	// Product Class Testing
	private static void BarCodeScannerReturnsString(){
		BarCodeScanner bcs = new BarCodeScanner();
		assertTrue("BarCodeScanner returns Strings", bcs.getScan().getClass().equals(String.class));
	}
	
	// Product Class Testing
	private static void getBarCodeReturnsString(){
		Product p = new Product();
		assertTrue("Product getBarCode returns Strings", p.getBarCode().getClass().equals(String.class));
	}
	private static void getBarCodeReturnsNotNull(){
		Product p = new Product();
		assertFalse("Product getBarCode returns Strings", p.getBarCode().equals(null));
	}

	private static void getPriceReturnsNotNegative(){
		Product p = new Product("aa", "aaa", 0.27);
		assertFalse("Product getBarCode returns Strings", p.getPrice() < 0);
	}
*/
}
