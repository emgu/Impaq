package tests;
/*
import static org.junit.Assert.*;
import static org.mockito.Mockito.*;

import java.io.PrintStream;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.Mock;
import org.mockito.Mockito;
import org.mockito.MockitoAnnotations;
//import org.mockito.MockitoAnnotations;

import database.DataBase;
import out.LCD;
import printbale.Product;
import store.*;

*/
public class testm {

/*
    @Mock
    PrintStream psMock;
    //Product p;
    //DataBase db;


    public testm() {
        MockitoAnnotations.initMocks(this);
    } 
    
    public void lcdtest(){
    //	PrintStream psMock = mock(PrintStream.class);
    	//@Mock
    	psMock = mock(PrintSteram.class);
    	verfy(psMock).println("dany tekst");
    	
    }
   /* 
    public void ProdWasFound(){
    	boolean q = true;
    	Mockito.when(p.wasFound()).thenReturn(q);
    }
    public void DBwrongFile(){
    	aaa
    	Mockito.verify(emailService, Mockito.atLeast(1)).sendEmail(Matchers.startsWith("admin@"), Matchers.anyObject());;
    }*/
}
